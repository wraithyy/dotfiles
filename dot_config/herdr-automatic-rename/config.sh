# herdr-automatic-rename overrides (sourced by naming.sh)
# Icons OFF: the glyph is written into the tab label itself, and herdr
# notifications reuse the label verbatim — Nerd Font glyphs render as tofu
# in macOS/phone notifications and there is no per-surface strip option.
ICONS_ENABLED=0
